---
# tasks file for dhall
- name: Convert expression to kubernetes objects
  dhall:
    expression: "{{ expression }}"
  register: _dhall_output

- name: Apply objects
  k8s:
    namespace: "{{ namespace }}"
    definition: "{{ _dhall_output.result }}"
